(function() {
    'use strict';
    angular.module('app')
        .factory('ThemeModel', ThemeModel);

    function ThemeModel() {
        var model = {

        };

        return model;
    }

})();
